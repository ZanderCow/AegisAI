# Use the official Postgres image as the base "infrastructure" for testing
FROM postgres:15-alpine

# Set environment variables for the testing database
# These distinguish the test environment from production/dev
ENV POSTGRES_DB=aegis_test_db
ENV POSTGRES_USER=test_admin
ENV POSTGRES_PASSWORD=test_password

# Copy your schema into the auto-initialization directory
# Any .sql scripts in this folder run automatically on first start
COPY schema.sql /docker-entrypoint-initdb.d/
