# This is an example environment file for the Vite frontend.
# Copy this to .env and adjust the variables as needed.

# The base URL for the backend API.
# Ensure this matches where your backend (e.g., FastAPI) is running.
VITE_API_BASE_URL=http://localhost:8000/api/v1
